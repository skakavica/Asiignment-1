import React from "react";

export default function funkciq1(props){
  function write(e){
    e = "Hello"
   
    }
  }
   return (
  <div>
    Refference:  {props.treatId = write(e)}
    CourseId:  {props.treatCourseId}
    Type: {props.type}
   category: {props.category} 
   Name: {props.name}
   startDate: {props.startDate}
 </div>
 

   )
;} 
 